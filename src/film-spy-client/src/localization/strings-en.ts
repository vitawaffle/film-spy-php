import type { StringsTemplate } from './strings';

const stringsEn: StringsTemplate = {
  common: {
    home: 'Home',
    logIn: 'Log in',
    logOut: 'Log out',
    register: 'Register',
    rooms: 'Rooms',
  },
};

export default stringsEn;
